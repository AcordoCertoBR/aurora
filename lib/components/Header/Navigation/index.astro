---
import type { HTMLAttributes } from 'astro/types'

export type Props = HTMLAttributes<'div'>

const { class: className, ...rest } = Astro.props
---

<div class:list={['au-header__navigation', className]} {...rest}>
  <slot />
</div>
